amqp_setup:
    1. defines channel, connection, exchange and queue properties
    2. function to create new exchange by providing room id (whenever a new room is created)
    3. function to create new queue by providing routing key (whenever a user joins a room chat)
    4. check_setup initiatializes channel, connection